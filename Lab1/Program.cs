namespace Lab1;

class Program
{
    static void Main(string[] args)
    {
     string name = "Jesse"
      int favNum = 64
      string favLang = "python"
      double progsBefore = 6
      bool hasEXP = true
      if hasEXP == true:
        string exp = "has"
      else:
        string exp = "does not have"
      Console.WriteLine(name + "'s favorite number is " + favNum + ". " + name + "'s favorite  programming language is " + favLang + name + "has written " + progsBefore +" programs" + " and " + exp + " programming experience")
    }
}
